{
  "version": "1.4.0",
  "minimumRequired": "1.0.0",
  "changelog": "Gift coupons: cart_rule now stores gift_product_attribute when the offered product is a specific combination, so customers receive the exact variation chosen by the merchant instead of the default combination."
}
